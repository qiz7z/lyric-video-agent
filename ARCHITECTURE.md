# ARCHITECTURE — 设计决策详解

> 面向技术读者（和面试官）。每一条决策都标注了它背后的实战教训。

## 0. 一句话定位

**多 Agent 协同的内容生产系统**：视频 Agent（正片）+ 封面 Agent（竖版封面），
共享确定性流水线骨架（11 级，每级幂等）；LLM 只在固定决策点出场（规划 / 对齐修复
/ 视觉质检 / 封面调研与文案），不做自由循环。

流水线总览（`orchestrator.py`，每级幂等，文件已存在即跳过 => 天然支持断点续跑）：

| Stage | 内容 | LLM 决策点 |
|---|---|---|
| 1 | 音频：ffmpeg 解码源音频为 wav（立体声，不写 -ac 1） | |
| 2 | 歌词：内嵌 LRC 优先 → LRCLib 兜底 | |
| 3 | 规划：主题/意象流/字体/段数 | ✅ Planner |
| 4 | 对齐 + 修复：demucs + DP三路路由；降级路由时 LLM function-calling 修复循环 | ✅ 修复循环 |
| 5 | 黑底白字验证片（便宜可重渲） | |
| 6 | **人工听感闸门**（确认对齐才进昂贵环节） | |
| 7 | Agnes 生片（限流/续传全封装） | |
| 8 | 视觉质检 + 重生成（≤2 轮） | ✅ 视觉QC |
| 9 | xfade 合成 + 烧字幕（NVENC→libx264 回退） | |
| 9.5 | **封面 Agent（并行支线）**：规划完成后即启动线程，与对齐/生片/合成重叠执行；调研→生图→文案→排版→QC，主链不依赖正片 | ✅ 封面×3 |
| 10 | 运行报告 + lessons 沉淀（记忆回写） | |

## 1. 为什么不用"LLM 驱动一切"的自由 Agent 循环

市面上的学生 Agent 项目大多是"LLM + 一堆工具 + while 循环"。这个项目刻意不这样做：

| 维度 | LLM 自由循环 | 本项目的混合式 |
|---|---|---|
| 成本 | 每步都过 LLM，一首歌上百次调用 | 每首歌 ~5~15 次（规划 1 + 修复决策 ≤3 + 质检 ≤2 + 封面调研 ≤4 + 文案 1） |
| 延迟 | 每步等推理；生片本身要 25-40 分钟 | 决策点之外零 LLM 开销 |
| 确定性 | 一条字幕的时间轴不该有随机性 | 对齐/合成是纯函数，可回归测试 |
| 可调试 | 失败要翻对话日志 | 流水线每级有落盘产物，单级可重跑 |

**原则：能用代码确定性解决的不用 LLM；LLM 出场的地方必须是真正需要判断力的。**

本项目中 LLM 的四个出场点：
1. **规划**（Planner）：从歌词情绪推导画面主题、意象弧线、每段 prompt、字体风格——
   这是创意判断，规则写不完。
2. **对齐修复**（RepairLoop）：对齐走降级路由时，LLM 通过 function calling
   诊断报告、选修复工具、读观察结果决定下一步——见 §4。
3. **视觉质检**（Verifier/封面QC）：抽帧判断"有没有人物/变形/离题/文字可读性"——
   过去只能人工终审，是全自动流程的最大断点。
4. **封面 Agent**（CoverAgent）：视觉选帧 + 文案创作 + 封面QC——见 §6。

## 2. 规划器：LLM 生成 + 确定性校验（生成-校验闭环）

`planner.py` 的关键设计：LLM 输出计划 JSON 后，`validate_plan()` 用代码做硬校验：

- **段数不由 LLM 定**：`clip_count_for(duration)` 按覆盖公式
  `N×15.06-(N-1)×0.3 ≥ 时长+2s` 计算。LLM 定段数必然偶发算错，错一段成片就比歌短。
- **内容安全不靠 LLM 自觉**：正则过滤人物词（people/girl/face...），命中即替换为
  中性风景。纯风景政策是生产复盘出的硬规则（人物必变形），不能指望模型每次遵守。
- prompt 数量不齐时用兜底意象补齐，并在 notes 里留 WARN 痕迹（可观测而不中断）。

这是"LLM 负责创意，代码负责约束"的具体形态。

## 3. 核心算法：字幕-人声对齐（项目的技术心脏）

### 3.1 问题

用户的唯一验收标准是"**唱一句出一句、唱完即清**"。LRC 时间轴标注的是歌词本
时间，与真实人声常有 0.3~5s 偏差且**非线性**（不同来源、不同段落偏差不同）。

### 3.2 演进史（每一版都是一次失败分析）

1. v1-v3：librosa 全曲能量/onset 检测 —— 伴奏鼓点假峰、短句间奏误判（+5~7s 错误）。
2. v4《寂寞沙洲冷》：demucs 人声分离 + 能量包络切演唱段 + **人工逐行核对**的
   LINE_SEG 映射表 —— 质量完美但不可扩展（每首歌半小时人工）。
3. v5-v6：自动映射（LRC 时刻就近匹配段）—— **失败**。LRC 时刻落在句间静音里
   导致认错段，31 段 vs 20 行数量不对等时贪心指针逐级错位，前半段整体漂移。
4. 均匀量化（本项目的第一版 sequential）：行 i 认领第 `i*m/n` 块段 —— **回归测试
   立刻复现 v5 漂移**：副歌长句跨 4 个演唱段，均匀假设从第 2 行起错位 12s+。
5. **最终方案：DP 单调对齐**（`align._align_sequential`）。

### 3.3 DP 单调对齐

把"行→段"映射建成带约束的最优对齐问题（类 DTW）：

- **状态**：`dp[i][j]` = 前 i 行消费前 j 段的最小代价
- **行 i 认领段 j..k-1 的代价**：`|segs[j].起点 - LRC_i| + |segs[k-1].末点 - LRC_{i+1}|`
  （双向锚定：行首贴 LRC 估计，行尾贴下一行估计——一行跨多段时自然被选上）
- **跳过段**（呼吸/噪声误检）：固定罚金 SKIP=8s（高于正常误差，低于错配代价）
- **单调性**：由状态转移天然保证，起点永不回漂

回归结果（两首歌、两类基准）：

- 《寂寞沙洲冷》：与**人工逐行校验的地面真值**对比，起点偏差中位数 **0.00s**
  （max 7.97s，来自长句子句拆分差异，人工闸门可兜住）；
- 《樱花草》：与**用户验收过的生产版 events** 对比，中位偏差 **0.03s**（86 段/64 行）。

对照组：均匀量化在寂寞沙洲冷上中位偏差 12.44s（见 §3.2 第 4 条）——
同一份数据、同一个真值，算法换代带来的是从"不可用"到"逐句精确"。

### 3.4 三路路由（不同歌的病理不同）

`ratio = 演唱段数 / 歌词行数` 决定走哪条路（`tools/align.py`）：

| 情形 | 路由 | 理由 |
|---|---|---|
| 1.0 ≤ ratio ≤ 2.0 | `sequential`（DP） | 段数≈行数，onset 是可靠骨架；DP 靠跳段罚金吸收多余段。
  上限实测从 1.7 放宽到 2.0：demucs 重跑段数 ±1 就会让边界歌翻路由（寂寞沙洲冷 34→35 段）， |
| ratio > 2.0 | `lrc_primary` | DJ 版/重复副歌：段远多于行，就近吸附会跨副歌
| ratio < 1 | `interp` | 段少于行（连唱/合句）：LRC 打底，报告提示人工锚点 |

`lrc_primary` 的 ±1.2s 吸附窗口来自实战：窗口开大后"距离最近"的贪心会把重复
副歌的行吸到上一轮副歌的 onset，再被单调链放大成 3~9s 暴偏。

### 3.5 事件化规则（唱完即清）

- `end = 下一句起点`（零空隙零重叠）
- 长间奏（句距>10s）：`end = start + 6s` 驻留即清
- **末句 `end = start + 6s`**，绝不挂片尾——曾因尾奏 20s 挂死同一句字幕返工
- 渲染只用 `\fad(150,180)+\be1`，**禁止 `\t` 动画**——即使封在 fade 窗口内，
  动画也会拉偏感知的出字边界

## 4. 修复循环：真 function-calling tool-use（对齐异常时）

触发：对齐走降级路由 interp（演唱段 < 歌词行数，字幕只能直用 LRC 时间）。

**LLM 的职责**：读对齐报告（路由/段数/偏差/长间奏）→ 通过 function calling
选择工具（`re_align(merge_gap/thr_low/thr_high)` 调分段灵敏度、`set_trim` 裁前奏）→
读执行观察结果 → 决定下一步或 `accept`。编排器执行工具并回填观察，
轮数上限 3。

**质量门槛（关键教训）**：实验发现把 merge_gap 从 0.30 调到 0.22 能让
《梦的光点》从 interp 升级到 sequential（56→64 段），但 delta_max 从 2.5s
**拉爆到 15.1s**——路由升级 ≠ 质量提升（段变碎后 DP 锚定反而选错段）。
所以候选结果必须通过 `delta_abs_max <= 3.0` 且字典序质量分
（路由优先、偏差次之）不劣化才被采纳；否则 REJECTED、自动回退，并把拒绝
原因回传给 LLM（提示不要原样重试）。

### 真模型实测（`agnes-2.5-flash`，《梦的光点》62 行 / 56 段）

复现：`tests/probe_repair_realmodel.py`。两次运行轨迹不同（LLM 采样随机性），
但都被门槛收敛到同一结果：

- **运行 A**：`re_align(merge_gap=0.22)` → 升级 sequential、dmax 拉爆到 15.138s
  → **REJECTED**；再 `set_trim(18)`、`re_align(merge_gap=0.28)` 均"未优于当前"
  → REJECTED；轮数上限 accept。
- **运行 B**：连续三次 `set_trim`（20 → 22 → 18），产出指标**完全相同**
  → 全部 REJECTED；轮数上限 accept。

**读出的三件事**：

1. 门槛确实在兜底——A 的 r1 复现了历史事故（路由升级 ≠ 质量提升）。
2. LLM 决策有随机性，所以质量门槛不是可选项而是必需品。
3. **参数抖动失效模式**：B 反复微调 `set_trim` 无效，根因是工具不对症
   （裁前奏解决不了"段数 < 行数"）+ 拒绝反馈只说"未优于当前"、没提示换工具。
   → Agent 的失败常常不是模型不聪明，而是**工具语义与反馈粒度不匹配**。

这个环节同时回答了"为什么不直接写死修复策略"：什么参数值得试依赖歌曲形态
（段/行比、前奏长度），正是 LLM 的判断力价比最高的地方；而门槛保证 LLM
判断错了也不会让结果变差。

## 5. 视觉质检：补上人工流程的断点

过去 25 首歌的流程是"人眼终审每一段画面"，原因是 AI 视频的人物变形无法自动判定。
本项目用视觉模型把这个环节自动化（`agent/verifier.py`）：

1. 每段抽 2 帧（5s/11s，避开转场）；
2. 视觉模型逐帧按三个检查项输出结构化结论（人物/变形/离题）；
3. 不合格的段：用模型给出的 `revised_prompt`（或追加负面反馈）重新生成；
4. 修复循环 ≤2 轮，防费用失控。

未配置视觉模型时优雅降级为"提示人工终审"，不阻塞流程。

## 6. 封面 Agent：多 Agent 协同（视频 Agent × 封面 Agent）

发布短视频平台必须有封面，而封面是**竖版 3:4**（1080×1440，主页网格显示比例），
正片是横版——不能截图了事，这是一个独立的生产任务。

### 6.1 为什么是两个 Agent，而不是一个 Agent 的两个阶段

面试必问。三条立论，每条都是真实差异：
- **模态不同**：视频 Agent 驱动视频模型；封面 Agent 协同图片模型 + 文本模型 +
  视觉模型，两个 Agent 各自的工具箱与失效模式完全不同。
- **QC 闭环不同**：正片查人物/变形/离题；封面查文字可读性/竖版构图/截断——
  检查项、失败原因、修复动作（重生成背景 vs 改写 prompt）均不相同。
- **生命周期不同**：封面 Agent 可独立对任意已有视频补跑（`--skip-generate`
  也能出封面），不需要重新生片——它是消费上游产物的独立服务，不是流水线的一个阶段。
  **此立论已在代码层面兑现**：`orchestrator._stage_cover_start` 在规划（Stage 3）
  完成后立即 `threading.Thread` 拉起封面支线，与对齐/生片/合成（Stage 4-9）重叠执行；
  封面主链（`run_headless`）只依赖歌名/歌手 + plan 主题，背景 prompt 来自调研产出，
  与正片是否存在无关。汇合点 `_stage_cover_join` 在报告前 `join()`，支线失败自动回退
  串行帧降级路径。

协同方式：共享产物（plan.json 的主题/意象、正片候选帧），无中心对话——
两个 Agent 通过文件系统交换结构化决策（cover_decision.json 记录每步的模式与理由）。

### 6.2 封面流水线（七步：先理解这首歌，再决定画什么）

0. **调研**[文本+工具循环]：把歌名/歌手交给文本模型，配 `search_web` 工具
   （ddgs 网页搜索直连可用）+ 预取的 MusicBrainz/mp3 元数据。模型自主决定搜什么
   （实测《梦的光点》自主搜了 1~4 次）、读结果、综合出 {背景, 视觉概念,
   英文生图提示词, 文案启发}。真实案例：搜出"动画《神兵小将》主题曲、
   林俊杰作曲"，视觉概念随之从"向日葵花田"（猜歌词）升级为"晨曦穿透云层，
   微弱光点汇聚成希望的脉络"（懂这首歌）。无 LLM/无网络 → 跳过，退回 plan 主题。
1. **候选帧抽取**：正片每段抽 1 帧（或整片均匀 6 帧）；
2. **选帧**[视觉]：视觉模型按"代表性/构图/标题留白"选代表帧；无视觉→中段帧启发式；
3. **竖版背景**：图片模型按**调研产出的生图提示词**生成海报背景（请求 1080x1440，
   模型按最近支持分辨率出图——实测 3:4 返回 864x1152，需 lanczos 放大）；
   无 key → 选中帧居中裁 3:4（纯代码降级）。**幂等键 = 提示词内容哈希**——
   调研产出新概念时自动失效旧背景缓存（同款缓存陷阱在验证片上踩过一次）；
4. **文案**[文本]：主标题（≤12 字情绪短句）+ 署名，输入含调研背景/启发
   （实测从"逆着光 也要开成向日葵"变为更贴题的"追着那束光出发"/"做那颗不灭的光点"）；
   无 LLM → 歌名/歌手兜底；
5. **排版渲染**：纯代码 drawtext + 华文行楷。**设计判断：模型负责画面，代码负责
   文字**——图像模型画中文几乎必乱码；
6. **封面QC**[视觉]：文字可读性/构图/人物检查；不合格带反馈重生成背景一次。

真三模型 E2E（梦的光点，含调研）：成品 `docs/cover_example.png`。

### 6.3 工程细节

- ffmpeg `drawtext` 的 Windows 转义重灾区（盘符冒号/反斜杠在 filtergraph 里
  极难逃净）→ 字体复制进工作区用相对文件名；textfile 同理；
- `cwd=workdir` 执行 ffmpeg 时，输入路径必须绝对（相对会以 workdir 为基准
  双重拼接——实测踩坑）；
- mock 模式强制禁用图片生成（离线测试绝不花钱），自动走帧降级路径。

## 7. 成本与可靠性工程

生片是全流程最贵的环节（每首 16-21 段 × 25-40 分钟）。三层防线：

- **人工闸门**（Stage 6）：黑底验证片（≈11MB，几秒渲完）确认对齐之后，
  才允许进入生片（200-330MB，半小时起）。字幕错了重渲验证片只要几秒，
  风景错了要重花钱。
- **幂等续传**：每级产物落盘，重跑自动跳过已完成部分。后台进程被系统回收
  （实测 ~50min 周期）后重跑命令即可，已生成片段不重复计费。
- **故障矩阵**（`videogen.py`，全部来自真实事故）：

| 故障 | 现象 | 对策 |
|---|---|---|
| 提交限流 | 429 queue.full | POST 也退避 65s 重试（≤60 次） |
| 轮询抖动 | SSL EOF / 503 | try/except + 退避，不快速重试 |
| 下载中断 | ChunkedEncodingError | Range 断点续传 + 80 次重试 |
| 假完成 | 下载完不是 mp4 | ftyp 头校验 + 大小下限校验 |
| 内容策略 | 400 | 换中性表述（400 不退避） |
| NVENC 失败 | 驱动/显存 | 自动回退 libx264 |

## 8. 记忆设计

两类记忆、两种生命周期（`agent/memory.py`）：

- **策略（playbook.md）**：长期、稳定、人工维护。规划时整篇注入——它编码的是
  "这个领域的物理规律"（限流参数、字幕禁令、路由选择），不该让模型每次重学。
- **经验（lessons.jsonl）**：短期、自动追加。每次运行结束记录路由/对齐质量/QC
  结果，下次规划取最近 5 条注入——上一首"哪类 prompt 触发了内容策略"这类
  近期事实，策略手册里不会有。

## 9. 测试策略

不依赖任何 API key 的离线回归（`tests/`），数据来自已交付歌曲；数据文件缺失时
自动 SKIP，因此同一套测试在本机（全量）与 CI（纯离线部分）都能跑：

- `test_align.py`：寂寞沙洲冷真实人声 + LRC，对比人工校验真值
  （中位偏差 0.00s）；同时断言路由选择、单调性、末句驻留等硬规则
- `test_lyrics.py`：三首真实 mp3 的内嵌 LRC 读取、LRCLib 兜底、脏数据过滤
- `test_orchestrator.py`：
  - ASS 渲染硬规则（禁 `\t`、fad+be、花括号转义、两套字号）
  - 规划校验（覆盖公式、prompt 数量、人物词净化）
  - **修复循环**：MockLLM 剧本 = 先 `re_align(merge_gap=0.22)`、被质量门槛
    REJECTED 后 `accept`——离线验证"尝试→被拒→回退→接受"完整路径
  - 真实 NVENC 合成小样（黑底验证片 + 2 段 xfade 正式片）
- `test_cover.py`：封面 Agent 离线全链——`run_headless` 入口两场景：①帧降级
  （MockLLM + 无图片 key，成品视频抽帧 + **真实 drawtext 排版** → 断言 1080×1440 PNG）；
  ②无视频产物（真图片 key 时 SKIP 守卫）验证"封面不依赖正片也能出"
- `probe_cover_headless.py`：真模型探针——仅歌名/歌手/plan 调 `run_headless()`
  无视频产物，验证并行支线立论；无 Agnes 图片 key 自动 SKIP

MockLLM 让规划/修复/封面链路在没有 key 时也可测通（`--mock`），这是 CI 友好性设计。
GitHub Actions（`.github/workflows/tests.yml`）在 push 时跑全部离线部分。

## 10. 已知边界（诚实声明）

- **LLM 实测状态**：修复循环与 Planner 均已完成真模型实测（Agnes `agnes-2.5-flash`，
  轨迹见 §4 与 `runs/梦的光点/repair_real_trajectory.json`）；封面链路早前已用
  Agnes 三真模型实测（选帧/文案/QC 全真，产物见 `docs/cover_example.png`）。
  尚未覆盖的部分：**视觉 QC** 需等生片后验证；真模型对 planner prompt 的
  JSON 漂移率尚未做多样本统计（目前 1~2 首样本均未出错）。
- **零 API 成本**：文本/视觉/图像/视频全部使用 Agnes 免费模型
  （`agnes-2.5-flash` / `agnes-image-2.1-flash` / `agnes-video-v2.0`）。
- 帧降级封面（无图片模型 key）若源视频带烧录字幕，选中帧会含字幕文字——
  决策记录里已标注模式，需人工终审。
- 对齐质量依赖 demucs 分离质量；强混响/现场版人声可能切碎，需人工锚点兜底。
- `interp` 路由（段少于行）只是 LRC 直通 + 近距吸附，未实现人工锚点插值 UI；
  修复循环目前只在 interp 路由触发。
- 视觉 QC 依赖多模态模型能力，对"轻微变形"的召回率有限——所以人工终审仍是
  建议步骤，QC 只是把明显问题拦在合成前。
- Agnes API 是本项目特定的视频/图片生成端点；换其他厂商只需替换 `videogen.py` 与 `imagogen.py`。
